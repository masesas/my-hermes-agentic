# Copy to .env on the VPS, then fill the values.
# Never commit the real .env.

# Public hostname that points to the VPS.
PUBLIC_DOMAIN=ai.example.com
PUBLIC_BASE_URL=https://ai.example.com
ADMIN_EMAIL=admin@example.com

# Public web server mode.
# caddy: repo manages Caddy on ports 80/443.
# nginx-direct: existing Nginx handles TLS/public traffic and proxies directly
# to 9Router at http://127.0.0.1:20128. In this mode, skip Caddy setup.
WEB_SERVER=nginx-direct

# 9Router dashboard/security.
NINE_ROUTER_INITIAL_PASSWORD=change-this-long-random-password
NINE_ROUTER_JWT_SECRET=change-this-64-char-random-secret
NINE_ROUTER_API_KEY_SECRET=change-this-64-char-random-secret
NINE_ROUTER_MACHINE_ID_SALT=change-this-64-char-random-secret

# Optional: force 9Router build/runtime to use a specific Node.js from nvm.
# Useful when root/user shells show Node 24 but sudo/systemd still resolves an
# older /usr/bin/node. Leave empty for auto-detection of Node >=20.9.0.
NINE_ROUTER_NODE_BIN=
NINE_ROUTER_NPM_BIN=

# API key generated from the 9Router dashboard after provider setup.
# 9Router API path is /api/v1 (not /v1). For localhost: http://127.0.0.1:20128/api/v1
NINE_ROUTER_API_KEY=
NINE_ROUTER_BASE_URL=http://127.0.0.1:20128/api/v1

# Model combo names configured inside 9Router Dashboard.
# Create one combo per profile and set the names here.
# These are injected into each profile's .env and config.yaml at seeding time.
# IMPORTANT: Hermes may not expand ${HERMES_MODEL} in config.yaml on some versions.
# The seed script (42-seed-profile-souls.sh) hardcodes these into config.yaml directly.
HERMES_MODEL_ORCHESTRATOR=morph-orchestrator
HERMES_MODEL_RESEARCHER=morph-researcher
HERMES_MODEL_EXECUTOR=morph-executor

# Discord bot token for Hermes gateway (legacy single-bot mode).
DISCORD_BOT_TOKEN=

# Per-profile Discord bot tokens (multi-agent mode).
# Each profile gets its own Discord bot for channel isolation.
DISCORD_BOT_TOKEN_ORCHESTRATOR=
DISCORD_BOT_TOKEN_RESEARCHER=
DISCORD_BOT_TOKEN_EXECUTOR=

# Comma-separated Discord user IDs allowed to interact with all bots.
# WARNING: some Hermes gateway versions treat empty as deny-all, not allow-all.
# Set your Discord user ID explicitly for production, e.g. DISCORD_ALLOWED_USERS=1234567890
DISCORD_ALLOWED_USERS=

# Owner channel mapping (Discord channel IDs, not names).
#
# Defines where each bot is the DEFAULT responder for un-mentioned messages.
# Bots remain @mentionable in ANY channel they have access to.
#
# Behavior:
#   - In its owner channel: bot auto-responds to un-mentioned human messages.
#   - In any other channel/thread: bot stays silent unless explicitly @mentioned.
#
# Caveat: this filter is enforced by the Hermes Discord gateway. If the running
# gateway version ignores this variable, the SOUL silence rules in each profile's
# SOUL.md remain the active guardrail. Leave empty to skip hard enforcement.
DISCORD_OWNER_CHANNELS_ORCHESTRATOR=
DISCORD_OWNER_CHANNELS_RESEARCHER=
DISCORD_OWNER_CHANNELS_EXECUTOR=

# Runtime identities.
HERMES_USER=hermes
HERMES_AGENT_NAME=hermes-orchestrator
